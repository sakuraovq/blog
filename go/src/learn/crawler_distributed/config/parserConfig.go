package config

const (
	// 获取城市列表parser
	ParserCityList = "GetCityList"

	// 获取城市parser
	ParserCity     = "GetCity"
	// 获取城市用户parser

	ParserProfile = "UserProfileParser"

	// null Parser
	NilParser     = "NilParser"
)
